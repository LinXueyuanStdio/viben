# Pet Package Core Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Verify and complete the foundational `packages/pet` core library with types, atlas processing, animation utilities, interaction state machine, and zustand store.

**Architecture:** Pure TypeScript library with zero UI dependencies. The core modules (`types.ts`, `atlas.ts`, `animation.ts`, `interaction.ts`, `store.ts`) are already implemented. This plan verifies they work correctly and adds any missing functionality.

**Tech Stack:** TypeScript, Zustand, Vitest

**Spec Reference:** `docs/superpowers/specs/2026-05-31-pets-v1-design.md`

---

## Current State

The core modules already exist and have unit tests:

```
packages/pet/
├── src/
│   ├── types.ts              # ✅ Complete - All type definitions
│   ├── atlas.ts              # ✅ Complete - Atlas loading, validation
│   ├── animation.ts          # ✅ Complete - Animation utilities
│   ├── interaction.ts        # ✅ Complete - Interaction state machine
│   ├── store.ts              # ✅ Complete - Zustand store
│   └── index.ts              # ✅ Complete - Exports
├── tests/
│   ├── atlas.test.ts         # ✅ Complete
│   ├── animation.test.ts     # ✅ Complete
│   ├── interaction.test.ts   # ✅ Complete
│   └── store.test.ts         # ✅ Complete
├── package.json              # ✅ Complete
├── tsconfig.json             # ✅ Complete
└── vitest.config.ts          # ✅ Complete
```

---

## Task 1: Verify Tests Pass

**Files:**
- Verify: `packages/pet/tests/*.test.ts`

- [ ] **Step 1: Run all unit tests**

Run:
```bash
cd packages/pet && pnpm test
```

Expected: All tests PASS (atlas, animation, interaction, store)

- [ ] **Step 2: Run type check**

Run:
```bash
cd packages/pet && pnpm typecheck
```

Expected: No errors

- [ ] **Step 3: Verify build**

Run:
```bash
cd packages/pet && pnpm build
```

Expected: Build succeeds, `dist/` directory created

---

## Task 2: Add Missing Utility - getAnimationDuration

**Files:**
- Modify: `packages/pet/src/animation.ts`
- Modify: `packages/pet/tests/animation.test.ts`

- [ ] **Step 1: Write failing test for getAnimationDuration**

Add to `packages/pet/tests/animation.test.ts`:

```typescript
describe('getAnimationDuration', () => {
  it('calculates duration in milliseconds', () => {
    const animation = { id: 'idle', row: 0, frames: 6, fps: 6 };
    expect(getAnimationDuration(animation)).toBe(1000); // 6 frames / 6 fps = 1s
  });

  it('handles different fps values', () => {
    const animation = { id: 'running', row: 1, frames: 8, fps: 8 };
    expect(getAnimationDuration(animation)).toBe(1000); // 8 frames / 8 fps = 1s
  });

  it('handles fractional durations', () => {
    const animation = { id: 'waving', row: 3, frames: 4, fps: 6 };
    const duration = getAnimationDuration(animation);
    expect(duration).toBeCloseTo(667, 0); // 4 frames / 6 fps = 0.667s
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run:
```bash
cd packages/pet && pnpm test
```

Expected: FAIL - getAnimationDuration not found

- [ ] **Step 3: Implement getAnimationDuration**

Add to `packages/pet/src/animation.ts`:

```typescript
/**
 * Calculate the total duration of an animation in milliseconds.
 */
export function getAnimationDuration(animation: PetAnimationDef): number {
  return Math.round((animation.frames / animation.fps) * 1000);
}
```

- [ ] **Step 4: Run test to verify it passes**

Run:
```bash
cd packages/pet && pnpm test
```

Expected: All tests PASS

- [ ] **Step 5: Commit**

```bash
git add packages/pet/src/animation.ts packages/pet/tests/animation.test.ts
git commit -m "feat(pet): add getAnimationDuration utility"
```

---

## Task 3: Add PetAtlasRowDef Type for Open-Design Compatibility

**Files:**
- Modify: `packages/pet/src/types.ts`

The existing code from open-design uses `PetAtlasRowDef` with `index` instead of `row`. Add this type for compatibility.

- [ ] **Step 1: Add PetAtlasRowDef type**

Add to `packages/pet/src/types.ts`:

```typescript
/**
 * Atlas row definition using 'index' field (open-design compatibility).
 * Maps to PetAnimationDef but uses 'index' instead of 'row'.
 */
export interface PetAtlasRowDef {
  index: number;
  id: string;
  frames: number;
  fps: number;
}

/**
 * Convert PetAtlasRowDef to PetAnimationDef.
 */
export function rowDefToAnimationDef(rowDef: PetAtlasRowDef): PetAnimationDef {
  return {
    id: rowDef.id,
    row: rowDef.index,
    frames: rowDef.frames,
    fps: rowDef.fps,
  };
}

/**
 * Convert PetAnimationDef to PetAtlasRowDef.
 */
export function animationDefToRowDef(animDef: PetAnimationDef): PetAtlasRowDef {
  return {
    id: animDef.id,
    index: animDef.row,
    frames: animDef.frames,
    fps: animDef.fps,
  };
}
```

- [ ] **Step 2: Run type check**

Run:
```bash
cd packages/pet && pnpm typecheck
```

Expected: No errors

- [ ] **Step 3: Commit**

```bash
git add packages/pet/src/types.ts
git commit -m "feat(pet): add PetAtlasRowDef type for open-design compatibility"
```

---

## Task 4: Final Verification

**Files:**
- Verify: all files in `packages/pet/src/`

- [ ] **Step 1: Run all tests**

Run:
```bash
cd packages/pet && pnpm test
```

Expected: All tests PASS

- [ ] **Step 2: Run type check**

Run:
```bash
cd packages/pet && pnpm typecheck
```

Expected: No errors

- [ ] **Step 3: Build package**

Run:
```bash
cd packages/pet && pnpm build
```

Expected: Build succeeds

- [ ] **Step 4: Verify exports**

Run:
```bash
ls packages/pet/dist/
```

Expected: `index.js`, `index.d.ts`, `types.js`, `types.d.ts`, etc.

- [ ] **Step 5: Commit**

```bash
git add -A packages/pet/
git commit -m "feat(pet): complete core library verification

Core modules verified:
- types.ts: PetConfig, PetAtlasLayout, PetInteraction, PetAtlasRowDef
- atlas.ts: validation, row selection, frame positioning
- animation.ts: ambient picker, CSS style generation, duration calculation
- interaction.ts: state machine, drag classification
- store.ts: zustand with localStorage persistence

All unit tests passing. Ready for components layer (Plan 2)."
```

---

## Summary

This plan verifies the existing core implementation and adds missing utilities:

| File | Status | Additions |
|------|--------|-----------|
| `types.ts` | ✅ Exists | + PetAtlasRowDef type |
| `atlas.ts` | ✅ Exists | - |
| `animation.ts` | ✅ Exists | + getAnimationDuration |
| `interaction.ts` | ✅ Exists | - |
| `store.ts` | ✅ Exists | - |
| `index.ts` | ✅ Exists | - |

**Next:** Plan 2 (Components) depends on this plan completing successfully.

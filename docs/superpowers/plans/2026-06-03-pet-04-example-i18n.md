# Pet Example i18n 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task.

**Goal:** 为 `packages/pet/example/` 添加 i18n 国际化支持

**Spec:** `docs/superpowers/specs/2026-06-03-pet-management-design.md`

**Architecture:** 使用 i18next + react-i18next 为 example 应用添加多语言支持，支持中英文切换。

**Tech Stack:** i18next, react-i18next, TypeScript

---

## 文件结构

```
packages/pet/example/src/
├── i18n/
│   ├── index.ts              # i18n 配置
│   └── locales/
│       ├── en.json           # 英文翻译
│       └── zh-CN.json        # 中文翻译
└── components/
    └── LanguageSwitcher.tsx  # 语言切换组件
```

---

## Task 1: 安装 i18n 依赖

**Files:**
- Modify: `packages/pet/package.json`

- [ ] **Step 1: 安装依赖**

Run: `cd /root/viben/packages/pet && pnpm add i18next react-i18next`
Expected: 依赖安装成功

- [ ] **Step 2: Commit**

```bash
git add packages/pet/package.json pnpm-lock.yaml
git commit -m "feat(pet-example): add i18n dependencies"
```

---

## Task 2: 创建英文翻译文件

**Files:**
- Create: `packages/pet/example/src/i18n/locales/en.json`

- [ ] **Step 1: 创建英文翻译**

```json
{
  "pet": {
    "title": "Pet Development",
    "subtitle": "Interactive testing environment for @viben/pet components",
    "selectPet": "Select Pet",
    "debug": {
      "title": "Debug Info",
      "interaction": "Interaction",
      "animation": "Animation",
      "fps": "FPS",
      "frames": "Frames",
      "frame": "Frame",
      "renders": "Renders",
      "paused": "Paused"
    },
    "frameControl": {
      "title": "Frame Control",
      "play": "Play",
      "pause": "Pause",
      "prevFrame": "Previous frame",
      "nextFrame": "Next frame"
    },
    "controls": {
      "resetPosition": "Reset Position",
      "hidePet": "Hide Pet",
      "showPet": "Show Pet",
      "toggleBubble": "Toggle Bubble",
      "copyLink": "Copy Link",
      "copied": "Copied!",
      "resetSettings": "Reset Settings"
    },
    "settings": {
      "size": "Size",
      "speed": "Speed",
      "animation": "Animation",
      "auto": "Auto",
      "constrainViewport": "Constrain to viewport"
    },
    "presets": {
      "title": "Position Presets",
      "topLeft": "Top Left",
      "topRight": "Top Right",
      "center": "Center",
      "bottomLeft": "Bottom Left",
      "bottomRight": "Bottom Right"
    },
    "help": {
      "title": "Help",
      "interactions": "Interactions",
      "interactionsText": "Click to toggle bubble\nDrag to move\nHover to wave\nWait 45s for idle animation",
      "keyboard": "Keyboard",
      "keyboardText": "Space: bubble | R: reset | T: theme | Esc: close sidebar | 0: auto | 1-9: animations"
    },
    "theme": {
      "light": "Light",
      "dark": "Dark"
    }
  }
}
```

- [ ] **Step 2: Commit**

```bash
git add packages/pet/example/src/i18n/locales/en.json
git commit -m "feat(pet-example): add English translations"
```

---

## Task 3: 创建中文翻译文件

**Files:**
- Create: `packages/pet/example/src/i18n/locales/zh-CN.json`

- [ ] **Step 1: 创建中文翻译**

```json
{
  "pet": {
    "title": "Pet 开发环境",
    "subtitle": "@viben/pet 组件交互式测试环境",
    "selectPet": "选择 Pet",
    "debug": {
      "title": "调试信息",
      "interaction": "交互状态",
      "animation": "动画",
      "fps": "帧率",
      "frames": "帧数",
      "frame": "当前帧",
      "renders": "渲染次数",
      "paused": "已暂停"
    },
    "frameControl": {
      "title": "帧控制",
      "play": "播放",
      "pause": "暂停",
      "prevFrame": "上一帧",
      "nextFrame": "下一帧"
    },
    "controls": {
      "resetPosition": "重置位置",
      "hidePet": "隐藏 Pet",
      "showPet": "显示 Pet",
      "toggleBubble": "切换气泡",
      "copyLink": "复制链接",
      "copied": "已复制！",
      "resetSettings": "重置设置"
    },
    "settings": {
      "size": "大小",
      "speed": "速度",
      "animation": "动画",
      "auto": "自动",
      "constrainViewport": "限制在视口内"
    },
    "presets": {
      "title": "位置预设",
      "topLeft": "左上",
      "topRight": "右上",
      "center": "居中",
      "bottomLeft": "左下",
      "bottomRight": "右下"
    },
    "help": {
      "title": "帮助",
      "interactions": "交互方式",
      "interactionsText": "点击切换气泡\n拖拽移动\n悬停挥手\n等待45秒进入空闲动画",
      "keyboard": "快捷键",
      "keyboardText": "空格: 气泡 | R: 重置 | T: 主题 | Esc: 关闭侧栏 | 0: 自动 | 1-9: 动画"
    },
    "theme": {
      "light": "浅色",
      "dark": "深色"
    }
  }
}
```

- [ ] **Step 2: Commit**

```bash
git add packages/pet/example/src/i18n/locales/zh-CN.json
git commit -m "feat(pet-example): add Chinese translations"
```

---

## Task 4: 创建 i18n 配置

**Files:**
- Create: `packages/pet/example/src/i18n/index.ts`

- [ ] **Step 1: 创建配置文件**

```typescript
// packages/pet/example/src/i18n/index.ts
import i18n from "i18next";
import { initReactI18next } from "react-i18next";
import en from "./locales/en.json";
import zhCN from "./locales/zh-CN.json";

const resources = {
  en: { translation: en },
  "zh-CN": { translation: zhCN },
};

// 检测浏览器语言
function detectLanguage(): string {
  const browserLang = navigator.language;
  if (browserLang.startsWith("zh")) {
    return "zh-CN";
  }
  return "en";
}

i18n.use(initReactI18next).init({
  resources,
  lng: detectLanguage(),
  fallbackLng: "en",
  interpolation: {
    escapeValue: false,
  },
});

export default i18n;
```

- [ ] **Step 2: Commit**

```bash
git add packages/pet/example/src/i18n/index.ts
git commit -m "feat(pet-example): add i18n configuration"
```

---

## Task 5: 创建语言切换组件

**Files:**
- Create: `packages/pet/example/src/components/LanguageSwitcher.tsx`

- [ ] **Step 1: 创建组件**

```tsx
// packages/pet/example/src/components/LanguageSwitcher.tsx
import { useTranslation } from "react-i18next";
import { Globe } from "lucide-react";

export function LanguageSwitcher() {
  const { i18n } = useTranslation();

  const toggleLanguage = () => {
    const newLang = i18n.language === "en" ? "zh-CN" : "en";
    i18n.changeLanguage(newLang);
  };

  return (
    <button
      className="lang-switch"
      onClick={toggleLanguage}
      title={i18n.language === "en" ? "切换到中文" : "Switch to English"}
    >
      <Globe size={16} />
      <span>{i18n.language === "en" ? "中文" : "EN"}</span>
    </button>
  );
}
```

- [ ] **Step 2: 添加样式到 App.css**

在 `packages/pet/example/App.css` 中添加：

```css
.lang-switch {
  display: flex;
  align-items: center;
  gap: 4px;
  padding: 6px 10px;
  border-radius: 6px;
  border: 1px solid rgba(255, 255, 255, 0.2);
  background: rgba(255, 255, 255, 0.1);
  color: inherit;
  font-size: 12px;
  cursor: pointer;
  transition: background 0.2s;
}

.lang-switch:hover {
  background: rgba(255, 255, 255, 0.2);
}
```

- [ ] **Step 3: Commit**

```bash
git add packages/pet/example/src/components/LanguageSwitcher.tsx packages/pet/example/App.css
git commit -m "feat(pet-example): add LanguageSwitcher component"
```

---

## Task 6: 集成 i18n 到 App

**Files:**
- Modify: `packages/pet/example/App.tsx`
- Modify: `packages/pet/example/main.tsx`

- [ ] **Step 1: 在 main.tsx 中导入 i18n**

在 `packages/pet/example/main.tsx` 顶部添加：

```typescript
import "./src/i18n";
```

- [ ] **Step 2: 在 App.tsx 中使用 useTranslation**

在 `packages/pet/example/App.tsx` 中：

1. 导入 hook 和组件：
```typescript
import { useTranslation } from "react-i18next";
import { LanguageSwitcher } from "./src/components/LanguageSwitcher";
```

2. 在组件内获取 t 函数：
```typescript
const { t } = useTranslation();
```

3. 替换硬编码文本为翻译键，例如：
```tsx
// Before:
<h1>Viben Pet Development</h1>

// After:
<h1>{t("pet.title")}</h1>
```

4. 在头部添加语言切换器：
```tsx
<header className="example-header">
  <h1>{t("pet.title")}</h1>
  <p>{t("pet.subtitle")}</p>
  <LanguageSwitcher />
</header>
```

- [ ] **Step 3: 验证构建**

Run: `cd /root/viben/packages/pet && pnpm build`
Expected: 构建成功

- [ ] **Step 4: 测试 example 应用**

Run: `cd /root/viben/packages/pet/example && pnpm dev`
Expected: 应用启动，可以切换语言

- [ ] **Step 5: Commit**

```bash
git add packages/pet/example/main.tsx packages/pet/example/App.tsx
git commit -m "feat(pet-example): integrate i18n into App component"
```

---

## Task 7: 更新现有组件使用翻译

**Files:**
- Modify: `packages/pet/example/src/components/PetManagerPanel.tsx`
- Modify: `packages/pet/example/src/components/PetEditorModal.tsx`
- Modify: `packages/pet/example/src/components/PetImportDialog.tsx`

- [ ] **Step 1: 更新 PetManagerPanel.tsx**

添加 `useTranslation` hook 并替换硬编码文本：

```typescript
import { useTranslation } from "react-i18next";

// 在组件内:
const { t } = useTranslation();

// 替换文本，例如:
// "Debug Info" -> t("pet.debug.title")
// "Animation:" -> t("pet.debug.animation") + ":"
```

- [ ] **Step 2: 更新其他组件**

对 `PetEditorModal.tsx` 和 `PetImportDialog.tsx` 执行类似操作。

- [ ] **Step 3: 验证构建**

Run: `cd /root/viben/packages/pet && pnpm build`
Expected: 构建成功

- [ ] **Step 4: Commit**

```bash
git add packages/pet/example/src/components/
git commit -m "feat(pet-example): add i18n to all example components"
```

# Pet Package Example App Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Create a standalone Vite development environment in `packages/pet/example/` for debugging and testing the pet components with unit tests and UI tests.

**Architecture:** Minimal Vite + React app that imports from `../src`, with demo pet spritesheets copied from the existing community pets collection.

**Tech Stack:** Vite, React 18, TypeScript, Vitest

**Spec Reference:** `docs/superpowers/specs/2026-05-31-pets-v1-design.md`

**Depends On:** Plan 1 (core) and Plan 2 (components) must be completed first.

---

## File Structure

```
packages/pet/example/
├── index.html
├── main.tsx
├── App.tsx
├── App.css
├── vite.config.ts
├── package.json
├── tsconfig.json
└── public/
    └── pets/
        └── tux/
            ├── pet.json
            └── spritesheet.webp
```

---

## Task 1: Example Package Setup

**Files:**
- Create: `packages/pet/example/package.json`
- Create: `packages/pet/example/tsconfig.json`
- Create: `packages/pet/example/vite.config.ts`

- [ ] **Step 1: Create example directory**

Run:
```bash
mkdir -p packages/pet/example/public/pets
```

- [ ] **Step 2: Create package.json**

```json
{
  "name": "@viben/pet-example",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.0",
    "react-dom": "^18.3.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.0",
    "typescript": "^5.4.0",
    "vite": "^5.4.0"
  }
}
```

- [ ] **Step 3: Create tsconfig.json**

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "jsx": "react-jsx",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "noEmit": true,
    "paths": {
      "@viben/pet": ["../src"],
      "@viben/pet/*": ["../src/*"]
    }
  },
  "include": ["*.tsx", "*.ts", "../src/**/*"]
}
```

- [ ] **Step 4: Create vite.config.ts**

```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import path from 'path';

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: {
      '@viben/pet': path.resolve(__dirname, '../src'),
    },
  },
  server: {
    port: 3456,
    open: true,
  },
});
```

- [ ] **Step 5: Commit**

```bash
git add packages/pet/example/package.json packages/pet/example/tsconfig.json packages/pet/example/vite.config.ts
git commit -m "feat(pet): setup example app with Vite config"
```

---

## Task 2: Copy Demo Pet Assets

**Files:**
- Copy: pet assets from existing community pets

- [ ] **Step 1: Copy tux pet from existing directory**

Run:
```bash
cp -r /root/github/others/open-design/assets/community-pets/tux packages/pet/example/public/pets/
```

- [ ] **Step 2: Verify pet.json exists**

Run:
```bash
cat packages/pet/example/public/pets/tux/pet.json
```

Expected: JSON with id, displayName, description, spritesheetPath

- [ ] **Step 3: Commit**

```bash
git add packages/pet/example/public/
git commit -m "feat(pet): add tux demo pet assets"
```

---

## Task 3: HTML Entry Point

**Files:**
- Create: `packages/pet/example/index.html`

- [ ] **Step 1: Create index.html**

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Viben Pet - Development</title>
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }
      body {
        font-family: system-ui, -apple-system, sans-serif;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        min-height: 100vh;
      }
      #root {
        min-height: 100vh;
      }
    </style>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/main.tsx"></script>
  </body>
</html>
```

- [ ] **Step 2: Commit**

```bash
git add packages/pet/example/index.html
git commit -m "feat(pet): add example HTML entry point"
```

---

## Task 4: Main Entry and App Component

**Files:**
- Create: `packages/pet/example/main.tsx`
- Create: `packages/pet/example/App.tsx`
- Create: `packages/pet/example/App.css`

- [ ] **Step 1: Create main.tsx**

```tsx
import { StrictMode } from 'react';
import { createRoot } from 'react-dom/client';
import { App } from './App';
import '@viben/pet/styles/pet.css';
import './App.css';

const root = document.getElementById('root');
if (root) {
  createRoot(root).render(
    <StrictMode>
      <App />
    </StrictMode>
  );
}
```

- [ ] **Step 2: Create App.css**

```css
.example-app {
  min-height: 100vh;
  padding: 24px;
  color: white;
}

.example-header {
  max-width: 600px;
  margin: 0 auto 40px;
}

.example-header h1 {
  font-size: 28px;
  font-weight: 600;
  margin-bottom: 8px;
}

.example-header p {
  opacity: 0.8;
  font-size: 14px;
}

.debug-panel {
  position: fixed;
  top: 24px;
  left: 24px;
  background: rgba(0, 0, 0, 0.8);
  border-radius: 12px;
  padding: 16px;
  font-size: 12px;
  font-family: ui-monospace, monospace;
  max-width: 300px;
  color: white;
}

.debug-panel h3 {
  font-size: 12px;
  font-weight: 600;
  margin-bottom: 12px;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  opacity: 0.7;
}

.debug-panel pre {
  white-space: pre-wrap;
  word-break: break-all;
  line-height: 1.5;
  color: #a0f0a0;
}

.debug-controls {
  margin-top: 16px;
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.debug-controls button {
  padding: 8px 12px;
  border: none;
  border-radius: 6px;
  background: rgba(255, 255, 255, 0.2);
  color: white;
  font-size: 12px;
  cursor: pointer;
  transition: background 0.2s;
}

.debug-controls button:hover {
  background: rgba(255, 255, 255, 0.3);
}

.instructions {
  position: fixed;
  bottom: 24px;
  left: 24px;
  background: rgba(0, 0, 0, 0.6);
  border-radius: 8px;
  padding: 12px 16px;
  font-size: 12px;
  line-height: 1.6;
  max-width: 240px;
  color: white;
}

.instructions strong {
  display: block;
  margin-bottom: 4px;
}

.error-message {
  color: #ff6b6b;
  padding: 20px;
  text-align: center;
}

.loading-message {
  color: white;
  padding: 100px;
  text-align: center;
  opacity: 0.7;
}
```

- [ ] **Step 3: Create App.tsx**

```tsx
import { useState, useEffect, useCallback } from 'react';
import {
  PetContainer,
  type PetConfig,
  type PetPosition,
  PET_DEFAULTS,
  STANDARD_ANIMATIONS,
} from '@viben/pet';

interface RawPetJson {
  id: string;
  displayName: string;
  description: string;
  spritesheetPath: string;
}

async function loadPetFromPublic(petId: string): Promise<PetConfig> {
  const configUrl = `/pets/${petId}/pet.json`;
  const response = await fetch(configUrl);
  if (!response.ok) {
    throw new Error(`Failed to load pet: ${response.status}`);
  }
  
  const raw = (await response.json()) as RawPetJson;
  const spritesheetUrl = `/pets/${petId}/${raw.spritesheetPath}`;
  
  return {
    id: raw.id,
    name: raw.displayName,
    description: raw.description,
    accent: '#f5a623',
    greeting: `Hi! I'm ${raw.displayName}. Try dragging me around!`,
    spritesheet: spritesheetUrl,
    atlas: {
      cols: 8,
      rows: 9,
      cellWidth: 192,
      cellHeight: 208,
      animations: STANDARD_ANIMATIONS,
    },
    ambient: PET_DEFAULTS.ambient,
    idleTimeoutMs: PET_DEFAULTS.idleTimeoutMs,
  };
}

export function App() {
  const [pet, setPet] = useState<PetConfig | null>(null);
  const [position, setPosition] = useState<PetPosition>(PET_DEFAULTS.position);
  const [showBubble, setShowBubble] = useState(true);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<string | null>(null);

  useEffect(() => {
    loadPetFromPublic('tux')
      .then(setPet)
      .catch((err) => {
        console.error('Failed to load pet:', err);
        setError(err.message);
      })
      .finally(() => setLoading(false));
  }, []);

  const handleTap = useCallback(() => {
    setShowBubble((prev) => !prev);
  }, []);

  const handleReset = useCallback(() => {
    setPosition(PET_DEFAULTS.position);
  }, []);

  const handleTogglePet = useCallback(() => {
    if (pet) {
      setPet(null);
    } else {
      setLoading(true);
      loadPetFromPublic('tux')
        .then(setPet)
        .catch((err) => setError(err.message))
        .finally(() => setLoading(false));
    }
  }, [pet]);

  if (loading) {
    return (
      <div className="example-app">
        <div className="loading-message">Loading demo pet...</div>
      </div>
    );
  }

  if (error) {
    return (
      <div className="example-app">
        <div className="error-message">Error: {error}</div>
      </div>
    );
  }

  return (
    <div className="example-app">
      <header className="example-header">
        <h1>Viben Pet Development</h1>
        <p>Interactive testing environment for @viben/pet components</p>
      </header>

      <div className="debug-panel">
        <h3>Debug Info</h3>
        <pre>
          {JSON.stringify(
            {
              pet: pet?.id ?? null,
              position,
              showBubble,
            },
            null,
            2
          )}
        </pre>
        <div className="debug-controls">
          <button onClick={handleReset}>Reset Position</button>
          <button onClick={handleTogglePet}>
            {pet ? 'Hide Pet' : 'Show Pet'}
          </button>
          <button onClick={() => setShowBubble((p) => !p)}>
            Toggle Bubble
          </button>
        </div>
      </div>

      <div className="instructions">
        <strong>Interactions:</strong>
        Click to toggle bubble<br />
        Drag to move<br />
        Hover to wave<br />
        Wait 45s for idle animation
      </div>

      <PetContainer
        pet={pet}
        position={position}
        onPositionChange={setPosition}
        onTap={handleTap}
        showBubble={showBubble}
      />
    </div>
  );
}
```

- [ ] **Step 4: Install dependencies**

Run:
```bash
cd packages/pet/example && pnpm install
```

- [ ] **Step 5: Commit**

```bash
git add packages/pet/example/main.tsx packages/pet/example/App.tsx packages/pet/example/App.css
git commit -m "feat(pet): add example App with debug controls"
```

---

## Task 5: Add Component Tests

**Files:**
- Create: `packages/pet/tests/components/PetSprite.test.tsx`
- Create: `packages/pet/tests/components/PetContainer.test.tsx`
- Modify: `packages/pet/vitest.config.ts`

- [ ] **Step 1: Update vitest.config.ts for React testing**

```typescript
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    globals: true,
    environment: 'jsdom',
    include: ['tests/**/*.test.ts', 'tests/**/*.test.tsx'],
    setupFiles: ['./tests/setup.ts'],
  },
});
```

- [ ] **Step 2: Create test setup file**

```typescript
// packages/pet/tests/setup.ts
import '@testing-library/react';
```

- [ ] **Step 3: Add testing dependencies**

Run:
```bash
cd packages/pet && pnpm add -D @testing-library/react jsdom
```

- [ ] **Step 4: Create components test directory**

Run:
```bash
mkdir -p packages/pet/tests/components
```

- [ ] **Step 5: Create PetSprite tests**

```tsx
// packages/pet/tests/components/PetSprite.test.tsx
import { describe, it, expect } from 'vitest';
import { render, screen } from '@testing-library/react';
import { PetSprite } from '../../src/components/PetSprite';
import type { PetConfig } from '../../src/types';
import { STANDARD_ANIMATIONS } from '../../src/types';

const mockPet: PetConfig = {
  id: 'test-pet',
  name: 'Test Pet',
  description: 'A test pet',
  accent: '#ff0000',
  greeting: 'Hello!',
  spritesheet: 'data:image/png;base64,test',
  atlas: {
    cols: 8,
    rows: 9,
    cellWidth: 192,
    cellHeight: 208,
    animations: STANDARD_ANIMATIONS,
  },
};

describe('PetSprite', () => {
  it('renders with pet config', () => {
    render(<PetSprite pet={mockPet} />);
    const sprite = document.querySelector('.pet-image.atlas');
    expect(sprite).toBeTruthy();
  });

  it('applies custom className', () => {
    render(<PetSprite pet={mockPet} className="custom-class" />);
    const sprite = document.querySelector('.custom-class');
    expect(sprite).toBeTruthy();
  });

  it('sets background image from spritesheet', () => {
    render(<PetSprite pet={mockPet} />);
    const sprite = document.querySelector('.pet-image.atlas') as HTMLElement;
    expect(sprite?.style.backgroundImage).toContain(mockPet.spritesheet);
  });

  it('accepts custom size', () => {
    render(<PetSprite pet={mockPet} size={64} />);
    const sprite = document.querySelector('.pet-image.atlas') as HTMLElement;
    expect(sprite?.style.width).toBe('64px');
    expect(sprite?.style.height).toBe('64px');
  });
});
```

- [ ] **Step 6: Create PetContainer tests**

```tsx
// packages/pet/tests/components/PetContainer.test.tsx
import { describe, it, expect, vi } from 'vitest';
import { render, screen, fireEvent } from '@testing-library/react';
import { PetContainer } from '../../src/components/PetContainer';
import type { PetConfig } from '../../src/types';
import { STANDARD_ANIMATIONS } from '../../src/types';

const mockPet: PetConfig = {
  id: 'test-pet',
  name: 'Test Pet',
  description: 'A test pet',
  accent: '#ff0000',
  greeting: 'Hello!',
  spritesheet: 'data:image/png;base64,test',
  atlas: {
    cols: 8,
    rows: 9,
    cellWidth: 192,
    cellHeight: 208,
    animations: STANDARD_ANIMATIONS,
  },
};

describe('PetContainer', () => {
  it('renders nothing when pet is null', () => {
    const { container } = render(<PetContainer pet={null} />);
    expect(container.querySelector('.pet-overlay')).toBeNull();
  });

  it('renders pet overlay when pet is provided', () => {
    render(<PetContainer pet={mockPet} />);
    const overlay = document.querySelector('.pet-overlay');
    expect(overlay).toBeTruthy();
  });

  it('shows bubble when showBubble is true', () => {
    render(<PetContainer pet={mockPet} showBubble={true} />);
    const bubble = document.querySelector('.pet-bubble');
    expect(bubble).toBeTruthy();
  });

  it('hides bubble when showBubble is false', () => {
    render(<PetContainer pet={mockPet} showBubble={false} />);
    const bubble = document.querySelector('.pet-bubble');
    expect(bubble).toBeNull();
  });

  it('displays pet name in bubble', () => {
    render(<PetContainer pet={mockPet} showBubble={true} />);
    const name = document.querySelector('.pet-bubble-name');
    expect(name?.textContent).toBe('Test Pet');
  });

  it('displays greeting in bubble', () => {
    render(<PetContainer pet={mockPet} showBubble={true} />);
    const content = document.querySelector('.pet-bubble-line');
    expect(content?.textContent).toBe('Hello!');
  });

  it('calls onTap when sprite is clicked', () => {
    const onTap = vi.fn();
    render(<PetContainer pet={mockPet} onTap={onTap} />);
    
    const sprite = document.querySelector('.pet-sprite');
    fireEvent.pointerDown(sprite!, { button: 0 });
    fireEvent.pointerUp(sprite!);
    
    expect(onTap).toHaveBeenCalled();
  });

  it('applies custom position', () => {
    render(
      <PetContainer 
        pet={mockPet} 
        position={{ right: 100, bottom: 200 }} 
      />
    );
    const overlay = document.querySelector('.pet-overlay') as HTMLElement;
    expect(overlay?.style.right).toBe('100px');
    expect(overlay?.style.bottom).toBe('200px');
  });
});
```

- [ ] **Step 7: Run tests**

Run:
```bash
cd packages/pet && pnpm test
```

Expected: All tests PASS

- [ ] **Step 8: Commit**

```bash
git add packages/pet/tests/ packages/pet/vitest.config.ts
git commit -m "feat(pet): add component tests for PetSprite and PetContainer"
```

---

## Task 6: Verify Example Runs

**Files:**
- Verify: all example files

- [ ] **Step 1: Start dev server**

Run:
```bash
cd packages/pet/example && pnpm dev
```

Expected: Browser opens at http://localhost:3456 with the pet visible

- [ ] **Step 2: Test interactions manually**

Manual testing checklist:
1. [ ] Verify pet displays with Tux spritesheet
2. [ ] Hover over pet - should wave (waving animation)
3. [ ] Click pet - bubble toggles
4. [ ] Drag pet right - shows running-right animation
5. [ ] Drag pet left - shows running-left animation
6. [ ] Drag pet up - shows jumping animation
7. [ ] Debug panel shows correct state
8. [ ] Reset Position button works
9. [ ] Hide/Show Pet button works
10. [ ] Toggle Bubble button works

- [ ] **Step 3: Commit**

```bash
git add -A packages/pet/example/
git commit -m "feat(pet): complete example app for development testing

- Vite dev server on port 3456
- Debug panel with state inspection
- Tux demo pet with full animation atlas
- Interactive controls for testing

Run: cd packages/pet/example && pnpm dev"
```

---

## Task 7: Final Verification

**Files:**
- Verify: all packages/pet files

- [ ] **Step 1: Run all unit tests**

Run:
```bash
cd packages/pet && pnpm test
```

Expected: All tests PASS (core + components)

- [ ] **Step 2: Run type check**

Run:
```bash
cd packages/pet && pnpm typecheck
```

Expected: No errors

- [ ] **Step 3: Build package**

Run:
```bash
cd packages/pet && pnpm build
```

Expected: Build succeeds

- [ ] **Step 4: Final commit**

```bash
git add -A packages/pet/
git commit -m "feat(pet): complete packages/pet with tests and example

Core:
- types.ts, atlas.ts, animation.ts, interaction.ts, store.ts

Components:
- PetSprite, PetBubble, PetContainer

Hooks:
- usePetDrag, usePetAmbient

Tests:
- Unit tests for core modules
- Component tests for PetSprite, PetContainer

Example:
- Standalone Vite app at packages/pet/example/
- Tux demo pet
- Debug panel for state inspection

All tests passing. Ready for desktop integration (Plan 5)."
```

---

## Summary

This plan creates the example app and adds component tests:

| File | Purpose |
|------|---------|
| `example/package.json` | Dependencies and scripts |
| `example/vite.config.ts` | Vite configuration with alias |
| `example/index.html` | HTML entry point |
| `example/main.tsx` | React entry point |
| `example/App.tsx` | Main app with debug controls |
| `example/App.css` | Styling |
| `example/public/pets/tux/` | Demo pet for testing |
| `tests/components/*.test.tsx` | Component unit tests |

**Usage:**
```bash
cd packages/pet/example
pnpm dev
```

**Next:** Plan 5 (Desktop Integration) should only proceed after all tests pass.

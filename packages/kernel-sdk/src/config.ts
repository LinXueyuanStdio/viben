// Config utilities for plugin settings and doctor checks
export type { ConfigFieldDef, DoctorReport, PendingFix } from '@viben/kernel'
export { DoctorEngine, getSafeFields, resolveOptions, getConfigValue, isHotReloadable } from '@viben/kernel'

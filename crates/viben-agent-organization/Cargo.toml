[package]
name = "viben-agent-organization"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "Viben Agent Organization - Initialize project structure for AI-assisted development workflow"

[lib]
crate-type = ["lib"]

[dependencies]
# Error handling
anyhow = { workspace = true }
thiserror = { workspace = true }

# Serialization
serde = { workspace = true }
serde_json = { workspace = true }

# Filesystem and utilities
chrono = { workspace = true }

# Hashing
sha2 = "0.10"
hex = "0.4"

[dev-dependencies]
tempfile = "3.10"
walkdir = "2.4"

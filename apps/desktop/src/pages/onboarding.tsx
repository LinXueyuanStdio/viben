import { OnboardingWizard } from "@/components/onboarding";

/**
 * Onboarding page - Full screen wizard for first-time setup.
 * Guides users through Python setup, Claude client detection, and login.
 */
export function OnboardingPage() {
  return <OnboardingWizard />;
}
